Utilities to import Montreal's 2013 election results and generate JSON of it and derived 'events'.

Setup:

    $ git clone -b 2013 git@github.com:elecnix/resultats-mtl-data.git history

Run:

    ./poll_media
